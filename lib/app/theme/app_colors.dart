import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Background
  static const background = Color(0xFF071721);
  static const surface = Color(0xFF102535);
  static const surfaceLight = Color(0xFF142C3E);

  // Primary
  static const primary = Color(0xFF29F2A6);
  static const primaryDark = Color(0xFF16C887);

  // Text
  static const textPrimary = Color(0xFFF5F7FA);
  static const textSecondary = Color(0xFF8FA3B5);
  static const textMuted = Color(0xFF60798C);

  // Status
  static const success = Color(0xFF29F2A6);
  static const warning = Color(0xFFF5B942);
  static const danger = Color(0xFFFF5C68);
  static const info = Color(0xFF4DB8FF);

  // Border
  static const border = Color(0xFF203B4D);
}